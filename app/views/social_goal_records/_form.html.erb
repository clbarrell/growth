<%= form_for(@social_goal_record) do |f| %>
  <% if @social_goal_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@social_goal_record.errors.count, "error") %> prohibited this social_goal_record from being saved:</h2>

      <ul>
      <% @social_goal_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :goal_id %><br>
    <%= f.text_field :goal_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
